# Contributing

## Development

Install dependencies:

pnpm install

Run library build in watch mode + demo app:

pnpm demo

## Code style

- TypeScript strict mode
- Keep runtime dependencies minimal
- Prefer provider-agnostic APIs
